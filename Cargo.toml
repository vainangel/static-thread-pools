[package]
name = "td"
version = "0.1.0"
edition = "2024"

[features]
default = ["dynamic_pool"]
fifo_pool = []
dynamic_pool = []

[dependencies]
priority-queue = "2.7.0"
rand = "0.10.1"
regex = "1.12.3"
